diff --git a/src/pages/Auth/Login.tsx b/src/pages/Auth/Login.tsx
index 1111111..2222222 100644
--- a/src/pages/Auth/Login.tsx
+++ b/src/pages/Auth/Login.tsx
@@ -46,6 +46,7 @@ const Login: React.FC = () => {
 
       if (success && data) {
         localStorage.setItem('token', data.token);
+        localStorage.setItem('user_level', data.level || '');
         localStorage.setItem('user', JSON.stringify({
           username: data.username,
           role: data.role,
diff --git a/src/layouts/LearnerLayout.tsx b/src/layouts/LearnerLayout.tsx
index 3333333..4444444 100645
--- a/src/layouts/LearnerLayout.tsx
+++ b/src/layouts/LearnerLayout.tsx
@@ -65,6 +65,7 @@ const LearnerLayout: React.FC = () => {
   const handleLogout = () => {
     localStorage.removeItem('token');
     localStorage.removeItem('user');
+    localStorage.removeItem('user_level');
     navigate('/login');
   };
 
diff --git a/src/pages/Dashboard/Dashboard.tsx b/src/pages/Dashboard/Dashboard.tsx
index 5555555..6666666 100646
--- a/src/pages/Dashboard/Dashboard.tsx
+++ b/src/pages/Dashboard/Dashboard.tsx
@@ -10,6 +10,7 @@ const Dashboard: React.FC = () => {
   const handleLogout = () => {
     localStorage.removeItem('token');
     localStorage.removeItem('user');
+    localStorage.removeItem('user_level');
     navigate('/login');
   };
 
diff --git a/src/layouts/AdminLayout.tsx b/src/layouts/AdminLayout.tsx
index 7777777..8888888 100647
--- a/src/layouts/AdminLayout.tsx
+++ b/src/layouts/AdminLayout.tsx
@@ -15,6 +15,7 @@ const AdminLayout: React.FC = () => {
   const handleLogout = () => {
     localStorage.removeItem('token');
     localStorage.removeItem('user');
+    localStorage.removeItem('user_level');
     navigate('/login');
   };
 
diff --git a/src/pages/Quizzes/QuizSession.tsx b/src/pages/Quizzes/QuizSession.tsx
index 9999999..aaaaaaa 100648
--- a/src/pages/Quizzes/QuizSession.tsx
+++ b/src/pages/Quizzes/QuizSession.tsx
@@ -53,6 +53,8 @@ const QuizSession: React.FC = () => {
     totalQuestions: number;
     assignedLevel: string;
     score: number | string;
+    passed?: boolean;
+    isPlacement?: boolean;
   } | null>(null);
 
   useEffect(() => {
@@ -124,26 +124,27 @@ const QuizSession: React.FC = () => {
         ? data.correctAnswers
         : Math.round((Number(score) / 100) * totalQuestions);
 
-      if (assignedLevel) {
-          localStorage.setItem('user_level', assignedLevel);
-      }
-
-      // Update level inside localStorage so layout updates instantly on transition
-      const userStr = localStorage.getItem('user');
-      if (userStr) {
-        try {
-          const userObj = JSON.parse(userStr);
-          userObj.level = assignedLevel;
-          localStorage.setItem('user', JSON.stringify(userObj));
-          // Dispatch custom event to trigger instant layout refresh
-          window.dispatchEvent(new Event('placement:completed'));
-        } catch (e) {
-          console.error("Failed to sync level to localStorage", e);
-        }
-      }
-      
-      // Dispatch event to trigger LearnerLayout re-render instantly without reload
-      window.dispatchEvent(new Event('placement:completed'));
+      // Only sync level to localStorage if we leveled up or if it is a placement test
+      if (assignedLevel && assignedLevel !== 'Chưa làm bài đánh giá') {
+        const oldLevel = localStorage.getItem('user_level');
+        if (isPlacement || oldLevel !== assignedLevel) {
+          localStorage.setItem('user_level', assignedLevel);
+          const userStr = localStorage.getItem('user');
+          if (userStr) {
+            try {
+              const userObj = JSON.parse(userStr);
+              userObj.level = assignedLevel;
+              localStorage.setItem('user', JSON.stringify(userObj));
+            } catch (e) {
+              console.error("Failed to sync level to localStorage", e);
+            }
+          }
+          window.dispatchEvent(new Event('placement:completed'));
+        }
+      }
+
+      // Determine pass/fail status
+      const passed = data.passed !== undefined ? data.passed : (Number(score) >= 50.0);
 
       setModalDetails({
         correctAnswers: correctAnswers || 0,
@@ -368,12 +368,21 @@ const QuizSession: React.FC = () => {
                     {modalDetails.score} điểm
                   </span>
                 </div>
-                <div className="flex justify-between items-center text-sm border-t border-slate-850 pt-3">
-                  <span className="text-slate-400 font-medium">🏆 Trình độ xếp lớp:</span>
-                  <span className="text-emerald-400 font-black text-base">
-                    Hạng {modalDetails.assignedLevel}
-                  </span>
-                </div>
+                {modalDetails.isPlacement ? (
+                  <div className="flex justify-between items-center text-sm border-t border-slate-850 pt-3">
+                    <span className="text-slate-400 font-medium">🏆 Trình độ xếp lớp:</span>
+                    <span className="text-emerald-400 font-black text-base">
+                      Hạng {modalDetails.assignedLevel}
+                    </span>
+                  </div>
+                ) : (
+                  <div className="flex justify-between items-center text-sm border-t border-slate-850 pt-3">
+                    <span className="text-slate-400 font-medium">🏆 Trạng thái:</span>
+                    <span className={`font-black text-base ${modalDetails.passed ? 'text-emerald-400' : 'text-red-400'}`}>
+                      {modalDetails.passed ? 'PASS' : 'NOT PASS'}
+                    </span>
+                  </div>
+                )}
               </div>
 
               <button
